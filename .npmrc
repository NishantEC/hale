link-workspace-packages = true

# Metro (the React Native bundler) can't follow pnpm's nested module
# store, so packages like whatwg-fetch (a transitive of @expo/metro-runtime)
# fail to resolve. Hoist everything into a flat node_modules/ to keep
# Metro happy under pnpm workspaces.
node-linker = hoisted
