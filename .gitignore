# Dependencies
node_modules
**/node_modules

# Build outputs
dist
**/dist
.turbo
**/.turbo
*.tsbuildinfo

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
.idea
*.swp
*.swo

# Env
.env
.env.local
.env.*.local

# Misc
*.local
out

# Pre-monorepo app/ directory — replaced by apps/app/ via git subtree.
# Safe to delete locally once apps/app/ is verified working.
/app/
/app-old/

# Tamagui's generated config — regenerated on each app build
**/.tamagui/
.expo/

.superpowers/

# Local Claude Code session state
.claude/
# Root app.json — the app's real config is at apps/app/app.json
/app.json
# Expo prebuild output — regenerated by `expo prebuild`
/ios/
/android/

# Vendored third-party WHOOP reverse-engineering references — kept locally,
# excluded from the published repo (foreign/unknown licenses).
/resource/
# Stray submodule reference (no .gitmodules) — kept locally, not in the repo.
/resources/

# Removed from the public repo (this repo is the app + Rust compute core only).
# Kept locally and archived in the private repo; not part of the published tree.
/apps/backend/
/apps/inspector/
/docs/
/infra/
/scripts/
/research/
/packages/

# Generated UniFFI bindings + prebuilt xcframeworks for the on-device compute
# module (~44 MB binaries each). Regenerate from apps/compute-engine — never
# committed.
/apps/compute-engine/bindings/
apps/app/modules/noop-compute/ios/NoopComputeFFI.xcframework/
